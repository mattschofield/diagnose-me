<%= form_for(@diagnosis_request) do |f| %>
  <% if @diagnosis_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diagnosis_request.errors.count, "error") %> prohibited this diagnosis_request from being saved:</h2>

      <ul>
      <% @diagnosis_request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
